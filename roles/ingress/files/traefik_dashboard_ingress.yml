apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: traefik-ingress
spec:
  rules:
  - host: traefik.k8smaster      
    http:
      paths:
      - backend:
          serviceName: traefik
          servicePort: traefik
